import State from '../core/State.js';
import List from '../core/List';

class Asset export State {
  constructor(state) {
    super({
      type: 'Asset',
      name: '',
      data: '',
    });
  }
}
